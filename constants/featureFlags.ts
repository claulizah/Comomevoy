/**
 * Perfil de moto es "Después"/premium según la sección 6 de la spec.
 * El storage y el hook (motoProfileStorage / useMotoProfile) ya existen,
 * listos para cuando se construya su pantalla — este flag es lo único
 * que falta prender.
 */
export const FEATURE_MOTO_PROFILE = false;
